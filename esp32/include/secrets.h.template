/**
 * secrets.h — Wi-Fi credentials (NOT committed to version control)
 *
 * Instructions:
 *   1. Copy this file to esp32/include/secrets.h
 *   2. Fill in your SSID and password below
 *   3. secrets.h is already in .gitignore — it will never be committed
 *
 * See docs/installation.md for full setup instructions.
 */
#pragma once

#define WIFI_SSID     "your_network_name_here"
#define WIFI_PASSWORD "your_password_here"
